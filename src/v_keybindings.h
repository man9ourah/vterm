/*
 * Modifier key
 */
#define VKEY_MODIFIER (GDK_CONTROL_MASK|GDK_SHIFT_MASK)

/*
 * VTerm keybindings
 */

/*
 * New tab
 */
#define VKEY_NEW_TAB GDK_KEY_t

/*
 * Next tab
 */
#define VKEY_NEXT_TAB GDK_KEY_l

/*
 * Previous tab
 */
#define VKEY_PREV_TAB GDK_KEY_h

/*
 * Alternate tab
 */
#define VKEY_ALTERNATE_TAB GDK_KEY_asciicircum

/*
 * Fast access 1
 */
#define VKEY_FAST_ACCESS_1 GDK_KEY_exclam

/*
 * Fast access 2
 */
#define VKEY_FAST_ACCESS_2 GDK_KEY_at

/*
 * Fast access 3
 */
#define VKEY_FAST_ACCESS_3 GDK_KEY_numbersign

/*
 * VTab keybindings
 */

/*
 * Close tab
 */
#define VKEY_CLOSE_TAB GDK_KEY_w

/*
 * Up prompt
 */
#define VKEY_UP_PROMPT GDK_KEY_k

/*
 * Down prompt
 */
#define VKEY_DOWN_PROMPT GDK_KEY_j

/*
 * Move tab to right
 */
#define VKEY_MOVE_TAB_RIGHT GDK_KEY_parenright

/*
 * Move tab to left
 */
#define VKEY_MOVE_TAB_LEFT GDK_KEY_parenleft

/*
 * Copy selected text to clipboard
 */
#define VKEY_COPY GDK_KEY_c

/*
 * Paste text in clipboard
 */
#define VKEY_PASTE GDK_KEY_v

/*
 * Increase font size
 */
#define VKEY_FONT_INCREASE GDK_KEY_plus

/*
 * Decrease font size
 */
#define VKEY_FONT_DECREASE GDK_KEY_underscore

/*
 * Reset font size
 */
#define VKEY_FONT_RESET GDK_KEY_bar
